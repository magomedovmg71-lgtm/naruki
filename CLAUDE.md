# НА РУКИ — правила проекта

## Что это

Демо-сайт для показа клиентам. Заведение «НА РУКИ» вымышленное: адрес,
отзывы и рейтинги придуманы. Телефон настоящий, +7 (922) 676-57-15 —
Магомеда: клиент с показа должен дозвониться, а не упереться в заглушку.
Внизу каждой страницы стоит пометка о вымышленности — **не убирать**, иначе
сайт читается как настоящий бизнес.

Название, город и адрес меняются заменой по всем HTML, если клиент хочет
увидеть свою вывеску.

## Чего здесь нет

- **Отправки заявок.** Заказ и бронь заканчиваются экраном «принято» и
  сообщением, что это демо. Реальная отправка — Cloudflare Worker в Telegram,
  как на magomedov.website.
- **Приёма платежей.** Выбор способа оплаты — интерфейс, эквайринга нет.
  На рабочем проекте это ЮKassa или CloudPayments плюс бэкенд.
- **Своих фотографий.** Стоят снимки с Unsplash — лицензия разрешает
  коммерческое использование без указания автора. Для реального клиента их
  меняют на съёмку заведения: та же папка, те же имена файлов.
- **Карточки в 2ГИС.** Встраиваемый виджет 2ГИС не стартует без ID организации
  в справочнике, поэтому на странице стоит виджет Яндекса, а 2ГИС — кнопкой
  наружу, поиском по адресу. Появится карточка у клиента — меняется `data-src`
  у `#map-live` и ссылка на 2ГИС на `/geo/<id>`.

## Ловушки

- **Меню рисует JS.** Без `menu-data.js` страница меню пустая. Поисковики
  рендерят JS, а разметка `Menu` в Schema.org продублирована в HTML — но
  если клиенту нужна гарантия, позиции придётся продублировать в разметке.
- **Данные меню в одном месте.** Дублировать позиции в HTML нельзя: разъедутся
  цены между главной и меню.
- **Комбо — не позиция меню.** В корзину кладётся id вида `combo:scramble+flat`,
  цена считается на лету: сумма двух блюд минус `COMBO_OFF` (28%) в `main.js`.
  Проценты в коде и надпись «−28%» на главной — два разных места, менять оба.
- **Часы акции — `COMBO_HOURS` в `menu-data.js`.** На демо стоит `[0, 24]`:
  блок живой в любое время, иначе после полудня клиент на показе видит серый
  прямоугольник. Механизм рабочий — вне окна селекты и кнопка блокируются,
  `data-add` снимается, под кнопкой появляется подпись. У реального клиента
  ставится настоящее окно, например `[8, 12]`, и тогда подпись в шапке блока
  («Комбо на завтрак») тоже правится под часы.
- **Скидку считает функция цены, а не выпадающий список.** Пара вне
  `COMBO_FOOD` или без кофе не оценивается вовсе, поэтому подставить
  `combo:crua+raf` через localStorage не выйдет.
- **Координаты заведения** стоят в Schema.org на двух страницах, в `data-src`
  карты и в ссылке на Яндекс Карты. Правятся вместе, иначе метка уедет от
  адреса — так и было: 42.9764/47.5024 указывали на Батырая вместо Коркмасова.
- **Корзина живёт в localStorage** под ключом `naruki-cart`. Между страницами
  переносится, между устройствами — нет.
- **Скидка за самовывоз 10%** обещана на `delivery.html` и считается в
  корзине. Меняете одно — меняйте второе.
- **Занятые столы в брони** вычисляются от выбранного часа, чтобы демо
  выглядело живым. Это не расписание, а имитация.
- **Блоки `.rise` невидимы до скролла.** Без JS их показывает `<noscript>`-стиль
  в `<head>`. Добавляя страницу, скопируйте его.
- **Появление блоков считает координаты, а не IntersectionObserver.** IO
  пропускал блоки при быстрой прокрутке, и они оставались невидимыми навсегда.
  Не возвращать обратно.
- **`[hidden] { display: none !important }` в начале style.css убирать нельзя.**
  У `.dish` есть `display: flex`, который перебивает браузерное правило — без
  этой строки фильтр меню проставляет атрибут, но карточки не скрываются.
- **Шрифты лежат в `assets/fonts/`, а не на Google.** Шесть файлов: кириллица,
  латиница и латиница-расширенная на каждое семейство. `@font-face` — в самом начале
  `css/style.css`, две кириллические подгружаются заранее через `preload` в `<head>`
  всех четырёх страниц. Вернёте ссылку на fonts.googleapis.com — вернёте два лишних
  соединения на первой загрузке. Меняете начертания — качать файлы заново, ссылка
  на генератор записана комментарием над `@font-face`.
- **Три картинки первого экрана грузятся без `loading="lazy"`.** Атрибут откладывал их
  до расчёта раскладки — донер и витрина стартовали на 380 мс позже стакана кофе.
  Ниже первого экрана `lazy` нужен, здесь — вреден.
- **Ширину страницы держит `html`, а не `body`.** `overflow-x: clip` плюс
  `overscroll-behavior-x: none` стоят на `html`: с `overflow-x: hidden` на `body`
  тот становится контейнером прокрутки и sticky-шапка перестаёт липнуть.
  `touch-action: manipulation` там же — снимает зум по двойному тапу на всех
  страницах сразу, включая корзину и диалоги.
- **Бургер включается с 1024px, а не с 860.** Между 861 и 1050 четыре пункта в
  строку не помещались: «Доставка и оплата» ломалась на две строки и рвала
  шапку. Подпись «Корзина» скрывается только до 560px.
- **`.svg` в MIME-таблице `server.js`** уже есть. Убирать нельзя — фавикон
  скачается вместо показа.
- **Цвет заглушек не равен цвету акцента.** `--ph-w1/--ph-w2` держат тёплый
  градиент еды отдельно от синего `--accent`: смешаешь их обратно — плашки
  уйдут в грязь. Проверено на синей палитре.
- **Акцент должен давать 4.5:1 с белым** — на нём стоит текст кнопок. Жёлтый
  и абрикосовый в `--accent` не годятся, им место в `--accent-soft`.
- **Цвета точек зон доставки** лежат в `js/menu-data.js`, а не в CSS. При смене
  палитры их правят руками.

## Аналитика

Счётчики не подключены: в `js/analytics.js` пустые `YM_ID` и `GA_ID`. Впишете
номер — код загрузится сам, ничего больше править не нужно. Цели уже
расставлены в коде: `add_to_cart`, `order_done`, `booking_done`, а клики по
телефону, WhatsApp, Telegram и картам ловятся автоматически по `href`.

## Проверка перед отправкой клиенту

`node server.js`, затем 1440 / 768 / 390 px: корзина, оформление на три шага,
смена способа получения, бронь, бургер-меню, консоль без ошибок.

Комбо проверяется в двух состояниях: на `COMBO_HOURS = [0, 24]` кнопка кладёт
пару в корзину со скидкой; временно поставить окно мимо текущего часа — селекты
заблокированы, под кнопкой подпись про часы. После проверки вернуть `[0, 24]`.
